# Copy to app/.env and adjust if needed. No secrets required — see CLAUDE.md.

# Path to the SQLite DB populated by the offline pipeline (pipeline/run_all.py).
# Defaults to db/nba_shot_quality.sqlite3 at the project root if unset.
DB_PATH=

# Flask debug mode (dev only — never enable in production).
DEBUG=false
